name: Terraform Apply

on:
  workflow_call:
    inputs:
      path:
        required: true
        type: string

jobs:
  tf-apply:
    uses: ./.github/workflows/tf/tf-apply.yml
    with:
      path: environment/staging/bigquery
      environment: staging
